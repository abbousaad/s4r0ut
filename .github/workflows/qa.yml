name: qa

on:
  push:
  pull_request:

jobs:
  dictionary-qa:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Run project QA
        run: make qa PYTHON=python
